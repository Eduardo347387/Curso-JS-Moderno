// Seleccionar varios elementos que tengan ese nombre

const card = document.querySelectorAll('.card')
console.log(card)

const formulario = document.querySelectorAll('#formulario')
console.log(formulario);

// Si un elemento no existe

const noExiste = document.querySelectorAll('no-Existe')
console.log(noExiste)

const formulario2 = document.getElementsByClassName('hacer')
console.log(formulario2);