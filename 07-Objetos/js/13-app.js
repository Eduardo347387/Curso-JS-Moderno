const producto = {
    nombre: "Monitor 20 pulgadas",
    precio: 300,
    disponible: true,
}

//retorna las llaves de un objeto
console.log(Object.keys(producto));

//Retorna lo valores del objeto\
console.log(Object.values(producto));

//Este retorna todo el objeto pero en pares
console.log(Object.entries(producto));

